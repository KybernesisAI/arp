{
  "name": "@kybernesis/arp-example-nanoclaw-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'example — no build' && exit 0",
    "test": "echo 'example — see tests/phase-6/adapter-conformance.test.ts' && exit 0",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@kybernesis/arp-adapter-nanoclaw": "workspace:*",
    "@kybernesis/arp-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
