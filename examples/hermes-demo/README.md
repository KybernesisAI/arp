# examples/hermes-demo

Minimal Hermes-Agent + ARP integration via `withArp(hermes, { handoff })`. Swap `FakeHermes` with `new HermesAgent(...)` once wired to a real Hermes-Agent install. See `../../adapters/hermes-agent/MIGRATION.md`.

Full conformance coverage: `tests/phase-6/adapter-conformance.test.ts`.
