{
  "name": "@kybernesis/arp-example-langgraph-research-agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'example — no build' && exit 0",
    "test": "echo 'example — see tests/phase-6/adapter-conformance.test.ts' && exit 0",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@kybernesis/arp-adapter-langgraph": "workspace:*",
    "@kybernesis/arp-sdk": "workspace:*",
    "@langchain/langgraph": "^0.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0"
  }
}
