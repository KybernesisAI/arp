# examples/openclaw-demo

Minimal OpenClaw + ARP integration via the `arpPlugin({...}).use(...)` pattern. Replace `FakeFramework` with `import { OpenClaw } from 'openclaw'` in a real deployment. See `../../adapters/openclaw/MIGRATION.md`.

Full conformance coverage: `tests/phase-6/adapter-conformance.test.ts`.
