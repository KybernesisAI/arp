/**
 * Re-export the canonical obligation schema from `@kybernesis/arp-spec`.
 * Kept as its own file so `types.ts` imports stay narrow.
 */
export { ObligationSchema, type Obligation } from '@kybernesis/arp-spec';
