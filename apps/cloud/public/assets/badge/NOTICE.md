card.glb — clip + clamp meshes used by /badge. From the react-three-fiber
interactive event badge example (Vercel blog, 2024; mirrored in
github.com/daffahaidar/interactive-3d-event-badge). Only the metal clip and
clamp nodes are rendered; the card body and prints are generated in code.
